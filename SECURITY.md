# 安全政策

## 支持范围

项目处于 Alpha 阶段。安全修复优先进入 `main`；发布正式版本后，将在此处列出仍受支持的版本。旧提交和非官方构建不承诺获得补丁。

## 私下报告漏洞

请不要为未修复的漏洞创建公开 Issue。仓库启用 GitHub Security Advisories 后，请使用 “Security → Report a vulnerability” 私下提交；若该入口尚不可用，请通过仓库所有者公开资料中的私密联系方式报告。

报告中请包含：

- 受影响的提交或版本；
- 最小复现步骤和实际影响；
- 所需前置条件；
- 可行的缓解方式（如已知）。

请勿附加真实书籍、个人学习记录、API Key 或其他人的数据。可以使用自编文本和最小化测试文件替代。

## 重点安全边界

- API Key 必须保存在 macOS 钥匙串，不得写入日志、设置文件或仓库。
- 云端 Provider 只能发送用户主动请求解释的文本及必要上下文，并应清晰展示目标服务。
- EPUB 属于不可信输入；资源解析必须限制在已打开书籍的容器内，不执行书内脚本，不允许目录穿越或隐式外部网络加载。
- PDF / EPUB 文件名、正文和学习记录属于用户私有数据，不应进入遥测或错误报告。
- JSON 持久化迁移必须保持数据完整性，并为损坏文件保留可恢复副本。

项目不会要求报告者为验证漏洞而访问无权使用的数据、破坏系统或影响其他用户。
