# JieJu TODO

任务按可并行模块组织。`[x]` 表示源码已实现并进入工程；真实模型质量和人工 UI 验收单独标记。

## Track A：语言引擎与评测

- [x] `AI-001`～`AI-006` 领域模型、协议、校验与 Codable 测试
- [x] `AI-101`～`AI-107` Mock、HTTP 抽象、Ollama 状态与错误测试
- [x] `AI-108` Ollama 服务、模型缺失与可用状态诊断
- [x] `AI-201`～`AI-208` Qwen 0.5B Prompt、严格 JSON、一次重试与解析测试
- [x] `AI-209` JSON 输入边界、temperature 0 与输出长度限制
- [x] `AI-210` 目标句引用一致性校验、上下文串线拦截与语义修复重试
- [x] `AI-211` JSON Schema 字段携带目标语言，修复 `translation` 输出英文而非中文
      （字段名本身对 0.5B/1.5B 构成词汇诱导，需连同字段描述一起处理）
- [x] `AI-212` 校验拦截 `translation` 与 `targetText` 完全相同
- [x] `AI-213` 修复轮逐项保留可信语法/短语，并强化中文解释约束
- [x] `AI-214` Ollama NDJSON 流式解码与增量 JSON 解析，最终结果继续执行严格校验
- [x] `AI-301`～`AI-309` Swift CLI、单句/上下文/JSON/raw/model/url
- [x] `AI-401`～`AI-402` JSONL 格式与 20 条冒烟评测句
- [ ] `AI-403` 扩展到 100 条正式评测句
- [x] `AI-404` 冒烟集覆盖主要语法与上下文类别
- [x] `AI-405`～`AI-408` 批量执行、结果、报告和人工评分标准
- [x] `AI-409` 运行有/无上下文真实模型对照
- [x] `AI-410` 记录 Qwen 0.5B 实测能力边界
- [x] `AI-411` 0.5B 与 1.5B 双模型批量对照，决定默认模型（1.5B，提交见 `a427e01` 之后）

> `AI-211`/`AI-212` 已于 2026-09-02 修复（提交 `a427e01`），
> `AI-409`～`AI-411` 的批量评测已产出有效数据
> （`Evaluation/results-{0.5b,1.5b}.jsonl` 与 `report-*.md`，均已 gitignore）。
> 剩余工作：人工评分 `Evaluation/report-*.md` 的 Manual scoring 表。

## Track B：PDF 阅读器

- [x] `PDF-001`～`PDF-008` 文档状态、文件面板、PDFKit 显示、页码与关闭
- [ ] `PDF-009` 固定测试 PDF 与自动打开 UI 测试
- [x] `PDF-101`～`PDF-110` 选区、文本清理、前后句、锚点、弹窗与状态
- [x] `PDF-113` 解句内容改为可滚动分区卡片，修复长文本拥挤、展开遮挡和弹窗高度问题
- [x] `PDF-114` 支持侧边栏/弹窗两种解句显示方式，并持久化用户选择
- [x] `PDF-115` 侧边栏模式始终保留解句区域，关闭解释时显示空状态，避免正文宽度跳变
- [x] `PDF-116` 阅读器自绘工具栏增加窗口拖动区域，恢复普通窗口移动
- [x] `PDF-117` 使用本地分词和词性把选区区分为词、表达、句子、段落或不确定；不确定时不再
  自动当作单词，并允许用户手动切换解释方式
- [x] `PDF-118` 收窄常驻解释栏、应用导航栏和资料列表的默认宽度，把主要空间还给正文
- [x] `PDF-119` 根据句内词法边界修复或提示不完整选区（如日语“行機”应建议“飛行機”），避免
  局部选中产生错误读音和原形；用户仍可保留原始选区
- [x] `PDF-111` 文本清理和上下文单元测试
- [ ] 人工验证跨页选择与不同 PDF 排版
- [x] 保存并恢复最近阅读页码
- [x] `PDF-112` 设置变化原位更新 AI 配置，不再重建 Reader 或丢失 PDF、页码与选区

## Track C：JSON 本地存储

- [x] `DATA-001`～`DATA-010` 版本化模型、原子写入、损坏恢复、CRUD、去重与测试
- [ ] 增加未来 schema version 的迁移策略
- [ ] 为文档生成内容指纹

## Track D：集成与学习闭环

- [x] `INT-001`～`INT-008` 可配置 Provider、设置、解释状态、取消、保存、去重、列表和删除
- [x] `INT-011` 设置页一键检查 Ollama 与默认模型
- [x] `INT-012` 设置页支持自定义解释语言（中/英/日/韩/法/德 + 自定义，默认中文）
- [x] `INT-013` 旧版 Mock 默认设置一次性迁移到本地 Ollama，真实 1.5B 解句成为默认
- [x] `INT-014` 解句面板逐步显示翻译、主干、语法点和短语；完成校验前禁止保存
- [x] `INT-015` 阅读器会话跨侧栏导航常驻，进入设置或学习记录后不关闭 PDF/EPUB
- [x] `INT-016` 保持 Ollama 为默认，并增加 OpenAI Chat Completions 兼容的云端 Provider
- [x] `INT-017` 设置页支持本地/云端切换、API 地址、模型名、连接检查和云端隐私提示
- [x] `INT-018` 云端 API Key 仅保存到 macOS 钥匙串，不进入 UserDefaults、JSON 或 Git
- [x] `INT-019` 云端解句支持 SSE 流式显示、严格结构校验和一次修复
- [ ] `INT-009` 重新解释已保存句子
- [ ] `INT-010` 完整端到端 UI 测试
- [x] 学习记录详情页（列表选择后展示原文、翻译、主干、语法、短语和来源）
- [x] 保存成功和失败的界面反馈
- [x] 学习记录删除补键盘/VoiceOver 可达路径（详情页提供显式删除按钮和确认）
- [x] 解释弹窗错误态补图标和文字标签，不仅依赖颜色传达

## 发布前关卡

- [x] App 与本地 Swift Package 共同构建
- [x] 语言引擎 57 项离线测试通过
- [x] 本机已安装 Ollama 与默认模型（0.5B、1.5B 均已拉取）
- [ ] 开启开发者模式（`sudo DevToolsSecurity -enable`，需用户执行并重启）
- [x] macOS App 单元测试恢复并通过 60 项
- [x] 恢复当前机器的 XCTest UI Runner 并重跑完整 UI 测试（2026-09-09：2 项通过）
- [x] 0.5B/1.5B 双模型冒烟评测（1.5B 定为默认模型，见 DECISIONS.md）
- [ ] 人工评分冒烟集输出（`Evaluation/report-*.md` Manual scoring）
- [ ] 在 M1 8GB 和至少一台更高配置 Mac 上记录延迟
- [ ] Xcode 人工运行与完整流程验收

## Track E：EPUB 正式阅读体验

现有 `NSTextView` 阅读器保留为“简洁/回退模式”；正式模式改用 WebKit 渲染原始 XHTML。

- [x] `EPUB-001` ZIP、container/OPF、spine 和章节纯文本解析核心
- [x] `EPUB-002` 简洁模式章节阅读、章节进度恢复和选区解句
- [x] `EPUB-201` 解析层暴露 XHTML、CSS、图片、字体等资源及其正确相对路径
- [x] `EPUB-202` 建立受限的 EPUB 本地资源访问层，禁止访问书外文件和网络
- [x] `EPUB-203` 用 `WKWebView` 渲染章节 XHTML，保留标题、强调、图片和原书基础 CSS
- [x] `EPUB-204` 注入阅读器 CSS，按视口多栏分页而非“一章一页”
- [x] `EPUB-205` 实现上一页/下一页、跨章衔接和键盘翻页
- [x] `EPUB-205A` 修复 WebKit 章内多栏宽度测量；顶部章节进度与章内页数分开显示
- [x] `EPUB-205B` 强制规范原章节正文视口并按横向滚动偏移翻页；撤销会导致空白的 DOM 搬运方案
- [ ] `EPUB-206` 计算当前章节页数、全书估算页数和重新排版状态
- [x] `EPUB-206A` 实时显示当前章节页数、页内进度条，并在图片/字体/窗口变化后重新计算
- [x] `EPUB-206B` 分页就绪前显示“正在排版”，过滤临时 1/1，并通过真实长章节回归
- [x] `EPUB-207` 使用章节 ID/资源路径 + 字符锚点保存稳定阅读位置，动态页码仅作显示与旧数据回退
- [x] `EPUB-207A` 保存和恢复章节索引 + 章内动态页码（稳定 DOM/字符锚点仍待 `EPUB-207`）
- [x] `EPUB-207B` 字号、行距、边距、侧栏和窗口重排使用可见正文锚点恢复，并通过真实长章节回归
- [x] `EPUB-208` JavaScript 选区桥接目标句、上下文、屏幕锚点及可序列化正文 locator
- [x] `EPUB-209` 复用现有侧边栏/弹窗解句及学习记录保存
- [x] `EPUB-210` 章节目录菜单和章节跳转
- [x] `EPUB-211A` 字号、行距、页边距设置及持久化
- [x] `EPUB-211B` 白纸、夜间、羊皮纸、护眼绿主题选择，显式覆盖书内前景与背景颜色
- [x] `EPUB-211C` 设置页提供实时排版预览，并强制正文段落响应字号和行距设置
- [ ] `EPUB-212` 脚注、内部链接和外部链接安全策略
- [ ] `EPUB-213` 保留简洁模式并支持与正式模式切换
- [ ] `EPUB-214` 固定 EPUB 的打开、分页、重排、翻章、恢复和选句 UI 测试
- [x] `EPUB-214A` 增加分页容器、逐页位移、长章节注入及 EPUB 阅读位置离线回归测试

## Track F：深度句法讲解

- [x] `AI-501` 扩展领域模型：句子类型、句型、成分、从句关系和整句理解路径
- [x] `AI-502` 每个句法成分携带原文片段、语法角色、修饰对象和中文说明
- [x] `AI-503` 校验句法原文片段必须来自目标句，防止上下文串线和虚构
- [x] `AI-504` 保留“快速解句”，增加按需触发的“深入解析”模式
- [x] `AI-505` 为深入解析设计独立短 Prompt、JSON Schema、解析与一次修复
- [x] `AI-506` 评测 1.5B 深入解析的结构正确率、JSON 成功率和延迟
- [ ] `AI-507` 对照 3B 模型；若 1.5B 高频误导，则将 3B 设为深度解析推荐模型
- [x] `AI-508` 解句面板按“翻译→句型→主干→成分→从句→语法”折叠展示
- [x] `AI-509` 为简单句、复合句、非谓语、倒装、省略和指代增加句法测试集

## Track G：日语假名与日语解句

- [x] `JA-001` 定义 `ReadingSegment(surface, reading)`，不以 HTML 作为持久化格式
- [x] `JA-002` 评估可离线嵌入的日语形态分析器与词典（准确率、体积、Swift 集成）
- [x] `JA-003` 使用 MeCab + IPADic 实现分词、词典读音、原形/活用识别和汉字—假名对齐
- [x] `JA-004` 建立多音字、姓名、活用、数字日期和熟字训评测集
- [x] `JA-005` 为 SwiftUI 解句面板实现 Ruby/Furigana 渲染组件
- [x] `JA-006` EPUB WebKit 正文支持原书 `<ruby>`，并为无注音文本安全注入本地读音
- [x] `JA-007` 增加“不显示/汉字注音”设置并持久化
- [x] `JA-008` 日语深度解释增加原形、读音、活用类型和句中语法功能
- [x] `JA-009` 模型只解释读音依据，不作为默认读音真值来源
- [x] `JA-010` 日语深入解析改为本地确定性语法层，消除 1.5B 超时并保证助词、句式和活用说明
- [x] `JA-011` 注音开关同时控制原书与自动 Ruby；读音节点不可选择且不进入 AI 上下文
- [ ] `JA-012` 真实 EPUB 验证注音开启后的系统选区和 AI 请求均不含假名（视觉文本方案已实现）
- [x] `JA-013` 自动注音仅对日语文档/章节启用；中文等非日语 EPUB 开启注音时不交给 MeCab 注入
- [x] `JA-014` 错标为英文/中文的日语 EPUB 可由强正文证据纠正，同时避免短日语引用误触发

正式 Provider 使用固定 revision 的 Mecab-Swift + IPADic；高频小词典仅作为词典初始化失败时的保守回退。
EPUB 注入跳过已有 `<ruby>`、脚本和样式；同一章节出现多个读音的相同表层词不自动注音。

## Track H：生词本与间隔复习

生词条目、来源语境和复习卡片分开建模。解句记录可以提供来源，但删除解句记录不得删除生词的
复习历史；模型候选词也不能未经用户确认自动加入生词本。

### H1：上下文单词解释

- [x] `WORD-001` 定义独立的 `WordExplanationRequest` / `WordExplanation`，包含原文词形、词典原形、
  读音、词性、当前语境义、简明释义、词形变化和常用搭配
- [x] `WORD-002` 定义独立 `VocabularyAI` 协议，复用 Ollama/OpenAI-compatible HTTP 层，但不扩大
  现有 `ReadingAI` 的职责
- [x] `WORD-003` 在重点表达、日语词形和经本地词法判断的词语选区上提供“解释单词”与“加入
  生词本”操作；句子和不确定选区不会直接写入生词本
- [x] `WORD-004` 日语读音、原形和活用优先使用本地 MeCab/IPADic；模型只补充当前语境义和说明
- [x] `WORD-005` 为结构化输出、目标词一致性、上下文串线、取消和流式预览增加离线测试

### H2：生词本

- [x] `VOCAB-001` 定义 `VocabularyEntry`、`VocabularySense` 和 `VocabularySource`；一个词可关联多个
  文档、句子和语境义
- [x] `VOCAB-002` 以“语言 + 规范化原形 + 可选读音”去重，保留不同表面词形和来源语境
- [ ] `VOCAB-003` 支持用户编辑词义、读音、标签和备注，并标明 AI 生成内容可修正
- [ ] `VOCAB-004` 创建生词本列表、搜索、筛选、详情和删除流程
- [x] `VOCAB-005` 新建 `learning-library` v2 schema 和 v1→v2 迁移；禁止直接修改已发布的 v1 schema
- [x] `VOCAB-006` 生词保存、合并、删除和迁移测试
- [ ] `VOCAB-007` 生词编辑、搜索和筛选测试

### H3：卡片与遗忘曲线

复习用于温和地帮助用户再次遇见学过的词，不构成每日任务。界面不得使用红点、催促通知、连续
打卡、积压总数或惩罚性文案；用户可以随时停止并回到阅读。

- [x] `SRS-001A` 定义独立 `ReviewCard` 并实现“词形→释义/读音”识别卡；收藏生词时自动建卡
- [ ] `SRS-001B` 增加“语境挖空→目标词”卡片及用户开关，避免默认把每日负担翻倍
- [x] `SRS-002` 定义 `Again / Hard / Good / Easy` 四档反馈、到期时间和可追加的 `ReviewLog`
- [x] `SRS-003` 抽象 `ReviewScheduler`，时钟可注入、结果确定可测试，不把调度逻辑写进 SwiftUI
- [ ] `SRS-004` 评估并固定首个 FSRS/Anki 风格调度版本；保留完整复习日志，以便以后更换算法重算
- [x] `SRS-005A` 实现到期队列；“随手温习”每轮默认 10 张，可自由继续或随时回到阅读
- [ ] `SRS-005B` 增加新卡/复习卡独立上限、暂停卡片和难词标记
- [x] `SRS-006` 实现识别卡正反面、显示答案、1～4 键评分和日语读音延迟揭示
- [x] `SRS-007A` 使用无压力文案呈现温习入口，不显示已复习数、积压数或完成率
- [ ] `SRS-007B` 仅提供用户主动打开的温和回顾，如“最近重遇的词”；不做连续天数、正确率、
  未来负担或欠卡统计
- [x] `SRS-011` 移除催促和惩罚性反馈：不发提醒、不设红点、不要求每日完成，反馈改为熟悉程度
- [x] `SRS-008A` 为首次学习、连续答对、遗忘重学、日志追加和 v1/v2→v3 迁移增加确定性测试
- [ ] `SRS-008B` 增加跨时区、夏令时和调度算法升级重算测试
- [ ] `SRS-009` 完成“划词→解释→收藏→到期复习→查看原句”的端到端 UI 测试
- [ ] `SRS-010` 后续评估 Anki/APKG 或通用 CSV 导入导出，不作为首版阻塞项

## Track I：跨平台基础

- [x] `XPLAT-001` 建立 `Shared/Contracts/v1`，覆盖 AI 请求/结果、深度分析、学习记录和 Reader Bridge
- [x] `XPLAT-002` 增加契约清单、安全字段检查、本地引用解析和 Swift 编码字段一致性测试
- [x] `XPLAT-003` 建立无依赖 `Shared/ReaderWeb` Bridge，统一 WKWebView/WebView2 消息封装
- [x] `XPLAT-004` 建立 `Apps/Windows` 边界、技术选型和首条垂直链路说明
- [x] `XPLAT-005` 将共享契约与 Reader Web 测试接入统一测试脚本
- [x] `XPLAT-006` 建立 Windows 真机环境、Solution 结构、契约版本、实施顺序和验收交接指南
- [ ] `XPLAT-101` macOS EPUB 改用统一 `jiejuBridge`，兼容迁移现有 selection/pagination handler
- [ ] `XPLAT-102` 将 EPUB 主题和排版 CSS 从 Swift 字符串迁入 `Shared/ReaderWeb`
- [ ] `XPLAT-103` 将分页状态机迁入共享 Web 层并补浏览器尺寸回归测试
- [ ] `XPLAT-104` 将选区、上下文清理和 Ruby 排除迁入共享 Web 层
- [ ] `XPLAT-105` macOS 已使用文本锚点保存稳定位置；仍需迁入共享 Reader Web，并评估 EPUB CFI 互操作
- [x] `WIN-001` 在 Windows 开发机创建并验证 WinUI 3 + WebView2 Solution
- [x] `WIN-101`～`WIN-105` 完成 Windows 原生导航、受限 EPUB 阅读、排版设置和最近位置恢复
- [x] `WIN-201` 完成 EPUB 划词、上下文提取、常驻解句栏、取消和错误状态
- [ ] `WIN-202` 已实现 Ollama NDJSON 流与结果校验；待安装本机 Ollama 后完成 5060 Ti 真机推理验收
- [ ] `WIN-002` 完成“打开 EPUB → 分页 → 选择 → Ollama → 保存”的 Windows 垂直闭环

## Track J：开源项目就绪度（不含 CI）

- [x] `OSS-001` 采用 Apache License 2.0，并补充 NOTICE
- [x] `OSS-002` 重写公开 README：能力、状态、安装、隐私、测试和限制
- [x] `OSS-003` 增加贡献指南、社区行为准则、安全政策和隐私说明
- [x] `OSS-004` 增加 Changelog 与手工签名、公证、发布检查清单
- [x] `OSS-005` 增加缺陷、功能、模型质量和 EPUB 兼容性 Issue 模板及 PR 模板
- [x] `OSS-006` 建立公开截图、测试文档和版权材料规范
- [ ] `OSS-007` 使用自编或公有领域文档制作 README 截图和短演示
- [ ] `OSS-008` 配置远程仓库后启用 Private Vulnerability Reporting，并替换 README 的源码获取说明
- [ ] `OSS-009` 审核第三方依赖许可证，生成随发布包分发的 attribution 清单
- [ ] `OSS-010` 完成首个签名、公证和校验和齐全的 Alpha Release

## Track K：书架、继续阅读与内容导航

- [x] `LIB-001` 建立最近文档模型，按路径去重并保留最多 12 本 PDF / EPUB
- [x] `LIB-002` 使用 security-scoped bookmark 保存持久访问权限；书籍移动或权限失效时提供重新定位
- [x] `LIB-003` 阅读空状态增加“继续阅读”和最近阅读列表，显示格式、最后位置与打开时间
- [x] `LIB-004` PDF 页码、EPUB 章节与章内页变化实时更新最近位置；关闭和重启后仍可继续打开
- [x] `LIB-005` 为持久化、去重、容量、失效书签、重新定位及 Reader 集成增加离线测试
- [ ] `LIB-006` 提取 EPUB 书名、作者和封面，形成完整书架而非仅显示文件名
- [ ] `LIB-007` 为文档生成内容指纹，移动或重命名后仍能关联阅读进度、学习记录和生词来源
- [ ] `NAV-001` 统一搜索当前 PDF / EPUB 正文，并可从结果跳回原文
- [x] `NAV-002` 学习记录、生词来源及温习卡片支持“回到原文”，复用稳定文档身份和内容锚点
- [ ] `MARK-001` 保存用户高亮、颜色与备注，并与 AI 解句记录保持可分离关系
- [ ] `MARK-002` 增加文内高亮列表、删除和跳转测试

## Later

- [ ] OCR
- [ ] 1.5B 质量回退建议
- [ ] 对 OpenAI 兼容云端服务做真实 API Key 端到端验收（当前仅离线协议测试）
- [ ] llama.cpp / MLX 本地 Provider 与 Ollama 延迟对照
- [ ] 数据导出与同步
